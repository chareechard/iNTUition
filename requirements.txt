beautifulsoup4>=4.7.1
requests>=2.22.0
lxml>=4.5.1

# test only
pytest>=7.0

# Google Drive pipeline (optional; only needed for drive_push / the dashboard's Push button)
google-api-python-client>=2.0
google-auth-oauthlib>=1.0

# PDF text (optional; needed by schedule_import for a STARS PDF, and by the Drive
# pipeline to extract text from PDFs before they reach the model)
pypdf>=4.0

# PDF page rendering (optional; lets Compendium show the model the actual rendered
# page image alongside extracted text - a self-contained wheel, no external poppler
# binary needed. Falls back to text-only when absent.)
pymupdf>=1.24

# Transcription (optional; only needed for transcribe_run / the dashboard's Whisper button)
faster-whisper>=1.0

# Research / AI panels (optional; needed by research_run and the dashboard's ask boxes)
anthropic>=0.40

# Desktop shell (optional; only needed to run or build the packaged iNTUition.exe)
pywebview>=5.0

# Inbound (optional; only needed for triage_run, which reads OWA in a browser).
# Also needs a one-time driver download: python -m playwright install chromium
playwright>=1.45
